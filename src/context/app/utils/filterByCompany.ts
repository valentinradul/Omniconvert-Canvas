
// This function is a placeholder since we're removing company functionality
// It just returns the original array since filtering is no longer needed
export const filterByCompany = <T>(items: T[], _companyId?: string): T[] => {
  return items;
};

export default filterByCompany;
